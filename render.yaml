services:
  - type: web
    name: varta-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.server:app -w 2 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    nativePackages:
      - name: ffmpeg
